{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block formulaire %}
<div class="container">
<div class="row" style="padding: 150px;">
<div class="formlog" style="margin-left: auto; margin-right: auto; width: 25em;">
<form method="post" style="background-color: #e8e8e54d;">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal" style="text-align: center; background-color: #35578c;
    color: #f4f4ee; border-radius: 2px; padding: 10px 10px;">Please sign in</h1>
    
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail"  placeholder="Email"
    class="form-control" required autofocus style=" margin-top: 3rem; width: 90%; margin-left: 20px;">
    
    <input type="password" name="password" id="inputPassword"   placeholder="Password"
    class="form-control" required=""
    style="margin-bottom: 3rem; margin-top: 3rem; width: 90%; margin-left: 20px;">

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-primary" type="submit" style="margin-bottom: 10px;
        padding: 5px 30px; margin-left: 25px; background-color: #6172c9; border: none;">
        log in
    </button>
</form>
            </div>
        </div>
    </div>
{% endblock %}
